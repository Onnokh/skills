---
name: example-skill
description: Placeholder skill demonstrating the repo layout. Replace or delete.
---

This is a template. Copy this folder, rename it, and rewrite the frontmatter
and body. The `description` is what the agent uses to decide when to load the
skill; the body is what it reads once loaded.
